
# NOTE: Replace this file on each machine with the generated hardware config:
#   sudo nixos-generate-config --show-hardware-config
#
# This stub exists so the flake evaluates if you download it before copying
# real hardware configs.

{ config, lib, pkgs, modulesPath, ... }:
{
}
